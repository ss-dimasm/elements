import { Story, Canvas, Controls, Meta } from '@storybook/blocks'
import { ToolTip } from './index'
import { RenderHtmlMarkup } from '../../storybook/render-html-markup'
import * as TooltipStories from './tooltip.stories'

<Meta of={TooltipStories} />

# Tool Tip

## Basic Usage

<Controls />

A tool tip is used for hovering over text and displaying additional info. Below is an example of what the tool tip looks like.

<Canvas of={TooltipStories.BasicUsage} />

<RenderHtmlMarkup of={TooltipStories.BasicUsage} />

## Default Active

If you want the initial state of the tooltip to be active, you can pass the `defaultActive` prop.

<Canvas of={TooltipStories.DefaultActive} />

<RenderHtmlMarkup of={TooltipStories.DefaultActive} />
