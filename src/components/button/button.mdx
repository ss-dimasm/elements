import { Meta, Story, Canvas, Controls } from '@storybook/blocks'
import { Button } from './index'
import { RenderHtmlMarkup } from '../../storybook/render-html-markup'
import * as ButtonStories from './button.stories'

<Meta of={ButtonStories} />

# Button

<Controls />

Styled button component with multiple flavours depending on use case.

Props available for the React version of the component;

## Default Usage

A button with no addtional props / classes applied.

<Canvas of={ButtonStories.DefaultUsage} />

<RenderHtmlMarkup of={ButtonStories.DefaultUsage} />

## With Intent

Different intents are supported, to enable buttons to be used in different contexts. Typcially you will always choose an intent for your button. The intent prop maps directly to the intent helper classes for non React users.

Whereas most components support the full range of intents, `Buttons` only support `primary`, `danger` and `default`, the latter being the default.

<Canvas of={ButtonStories.Intent} />

<RenderHtmlMarkup of={ButtonStories.Intent} />

## Is Loading

Add a loading spinner to indicate progress of a background activity after the button was clicked.

<Canvas of={ButtonStories.Loading} />

<RenderHtmlMarkup of={ButtonStories.Loading} />

## Is Disabled

Indicate that a button is disabled for whatever reason.

<Canvas of={ButtonStories.Disabled} />

<RenderHtmlMarkup of={ButtonStories.Disabled}/>

## Other attributes

All other standard HTML attributes for `<button>` are supported and are passed through to the React component, e.g. type and onClick

<Canvas of={ButtonStories.StandardAttributes} />

<RenderHtmlMarkup of={ButtonStories.StandardAttributes} />

## Button Group

An element to collectively group several buttons

<Canvas of={ButtonStories.Group} />

<RenderHtmlMarkup of={ButtonStories.Group} />

## Button Size

If you wish to size up or down a button, you can use the `buttonSize` prop.

By default all of our buttons are sized to `small` in mobile and `medium` in desktop so this option is solely if you wish to force the size.

<Canvas of={ButtonStories.ButtonSize}/>

<RenderHtmlMarkup of={ButtonStories.ButtonSize}/>

## Button Icons

If you wish to add a system icon from the `IconList`, you can do this by passing the `buttonIcon` prop.

You have the option to specify a position of `left`, `right` or `only` as a postion for your icon.

<Canvas of={ButtonStories.ButtonIcons}/>

<RenderHtmlMarkup of={ButtonStories.ButtonIcons} />

## Floating Button

A circular floating icon button - supports any icon from the icon list.

Props available for the React version of the component are the same as the main button.

<Canvas of={ButtonStories.Floating}/>

<RenderHtmlMarkup of={ButtonStories.Floating} />
